{
  "name": "locally-api",
  "version": "1.0.0",
  "description": "API functions for LOCALLY app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "dotenv": "^16.3.1"
  }
}
